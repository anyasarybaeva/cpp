#include<iostream>
int main()
{
	std:string	command;
	
	while(1)
	{
		std::cout<<"PhoneBook:";
		std::cin>>command;
		if(command=="ADD")
		{
		}
		else if(command=="SEARCH")
		{
		}
		else if(command=="EXIT")
		{

		}

	}
}
