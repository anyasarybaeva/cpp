#include<iostream>
int main()
{
	std::string	command;
	std::int index;
	std::string
	std::cout<<"PhoneBook:";
	while(getline(std::cin,command))
	{
		if(command=="ADD")
		{
			command_add()
		}
			/*else if(command=="SEARCH")
			{
			}*/
		else if(command=="EXIT")
		{
			std::cout<<"exit"<<std::endl;
			break;
		}
		std::cout<<"PhoneBook:";	
	}
}
